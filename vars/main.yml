alertmanager_package: "alertmanager-{{ alertmanager_version }}.linux-amd64.tar.gz"
alertmanager_url: "https://github.com/prometheus/alertmanager/releases/download/v{{ alertmanager_version }}/{{ alertmanager_package }}"
