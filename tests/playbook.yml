---

- hosts: alertmanager
  roles:
    - prometheus_alertmanager-role
  post_tasks:
    - name: POST_TASKS | Check if Alertmanager port is up
      wait_for:
        port: "{{ alertmanager_port }}"
        delay: 10
        timeout: 30
